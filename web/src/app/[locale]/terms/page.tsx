import type { Locale } from "@/content/schema";
import { pageMetadata } from "@/lib/site";
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) { const locale = (await params).locale as Locale; return pageMetadata(locale, "/terms", "Terms"); }
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const locale = (await params).locale as Locale; return <div className="prose-page"><p className="kicker">TERMS / RELEASE CANDIDATE</p><h1>Terms</h1><div className="notice"><strong>{locale === "en" ? "Placeholder—not legal advice or final terms." : "Placeholder — 법률 자문이나 최종 약관이 아닙니다."}</strong><p>{locale === "en" ? "No production service, account, payment, subscription, or user-submission terms are asserted in this release candidate. Final terms require review against the actual public service before release." : "이 출시 후보는 운영 서비스, 계정, 결제, 구독, 사용자 제출에 관한 약관을 주장하지 않습니다. 공개 전 실제 서비스 범위에 맞춘 최종 검토가 필요합니다."}</p></div></div>; }

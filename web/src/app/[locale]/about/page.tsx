import type { Locale } from "@/content/schema";
import { pageMetadata } from "@/lib/site";
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) { const locale = (await params).locale as Locale; return pageMetadata(locale, "/about", locale === "en" ? "About" : "소개"); }
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const locale = (await params).locale as Locale; return <div className="prose-page"><p className="kicker">ABOUT</p><h1>{locale === "en" ? "A mystery publication that shows its work" : "검증 과정을 보여주는 미스터리 퍼블리케이션"}</h1><p>{locale === "en" ? "Mystery Atlas pairs editorial storytelling with scoped evidence records. It is designed for readers who want both the story and a clear answer to what the available sources actually support." : "Mystery Atlas는 편집 서사와 범위를 한정한 근거 레코드를 결합합니다. 이야기와 함께 현재 출처가 실제로 어디까지 뒷받침하는지 알고 싶은 독자를 위한 제품입니다."}</p><h2>{locale === "en" ? "What it is not" : "하지 않는 것"}</h2><p>{locale === "en" ? "It is not a game, a certainty score, an automatic fact-checker, or a claim that every mystery can be kept continuously current." : "게임, 신뢰도 점수, 자동 팩트체커가 아니며 전 세계 모든 미스터리를 항상 최신으로 유지한다고 약속하지 않습니다."}</p></div>; }

import type { Locale } from "@/content/schema";
import { pageMetadata } from "@/lib/site";
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) { const locale = (await params).locale as Locale; return pageMetadata(locale, "/privacy", "Privacy"); }
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const locale = (await params).locale as Locale; return <div className="prose-page"><p className="kicker">PRIVACY / RELEASE CANDIDATE</p><h1>Privacy</h1><div className="notice"><strong>{locale === "en" ? "Placeholder—not legal advice or a final policy." : "Placeholder — 법률 자문이나 최종 정책이 아닙니다."}</strong><p>{locale === "en" ? "This release candidate stores saved case identifiers in this browser’s local storage. It does not include accounts, forms, advertising, or configured analytics. A final policy must match the actual production hosting and analytics configuration before public release." : "이 출시 후보는 저장한 사건 식별자를 브라우저 localStorage에 보관합니다. 계정, 입력 폼, 광고, 설정된 분석 도구는 포함하지 않습니다. 공개 전에 실제 운영 호스팅 및 분석 설정과 일치하는 최종 정책이 필요합니다."}</p></div></div>; }
